import 'package:flutter/material.dart';

class FolderGrid extends StatelessWidget {
  const FolderGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Folder Grid'),
    );
  }
}
