import 'package:flutter/material.dart';

class ShowModalSearchname extends StatelessWidget {
  const ShowModalSearchname({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Text 132'),
    );
  }
}
